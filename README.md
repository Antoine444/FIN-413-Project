# FIN-413-Project

## Setup

1. Clone the repository and navigate into it
2. Run `bash setup.sh`
3. Copy your Alchemy RPC URL into a `.env` file: `RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY`
4. Run `python data_extraction.py`
